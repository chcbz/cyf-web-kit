# Obsolete Juyiting modular-layer manifest note

This historical draft has been superseded by the TMX-driven map resource contract.

Current rule: `public/juyiting/hall.tmx` is the single source of truth for map image layers, tilesets, and prop images. Do not create JS map-layer manifests; update TMX instead. The old entrance prop was removed from runtime assets.
