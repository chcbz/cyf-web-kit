#!/usr/bin/env node
import { readFileSync } from 'node:fs'
import { createHash } from 'node:crypto'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
import { PNG } from 'pngjs'
import { atomicWriteUtf8Batch } from './lib/atomic-write.mjs'
import { readGitBlobAtCommit } from './lib/baseline-provenance.mjs'
import { renderMaskContactSheet } from './render-mask-contact-sheet.mjs'
import {
  ZERO_GENERATION_ID, TMX_SHA256, E9A_GENERATION_ID, E9B_COMMIT, BASE_COMMIT, NAV_AREA,
  OWNER_BY_MASK, VISUAL_BY_MASK, PROBE_FIXTURES, RECALIBRATIONS, sha256, stableJson,
  countOwnedPixelsInPolygon, pointInPolygonInclusive, pointStatus
} from './lib/mask-migration-evidence.mjs'

const __dirname=dirname(fileURLToPath(import.meta.url)),REPO_ROOT=join(__dirname,'..','..')
const PATHS={inventory:'tests/fixtures/juyiting/occlusion-v0/inventory.json',fragment:'tests/fixtures/juyiting/occlusion-v2-fragments/fragment-ownership-spec.json',prop:'tests/fixtures/juyiting/occlusion-v1-props/prop-sort-spec.json',atlas:'tests/fixtures/juyiting/occlusion-v2-atlases/atlas-manifest.json',e8b:'tests/fixtures/juyiting/occlusion-v1-props/prop-tmx-manifest.json',e8aContact:'tests/fixtures/juyiting/occlusion-v1-props/contact-sheet.svg',tmx:'public/juyiting/hall.tmx',canonical:'public/juyiting/images/liangshan-hall-mid-occluders-v3.webp',base:'public/juyiting/images/liangshan-hall-base-clean-v3.webp',prop92:'public/juyiting/images/props/liangshan-hall-prop-bounty-board-cropped.png',ledger:'tests/fixtures/juyiting/occlusion-v2-masks/migration-ledger.json',contact:'tests/fixtures/juyiting/occlusion-v2-masks/contact-sheet.svg'}
const read=p=>readFileSync(join(REPO_ROOT,p)),json=p=>JSON.parse(read(p)),frozen=p=>readGitBlobAtCommit('7404d361daba8f0af0dea98ab9db38cbbf01b286',p)
export async function generateMaskMigrationFixtures({write=true}={}){
 const inventory=json(PATHS.inventory),fragSpec=json(PATHS.fragment),propSpec=json(PATHS.prop),atlas=json(PATHS.atlas)
 const { buildInventory } = await import('./inventory-juyiting-map.mjs')
 const liveGeometry=buildInventory(read(PATHS.tmx).toString('utf8'),{baselineCommit:inventory.baselineCommit})
 if(stableJson(liveGeometry.collision)!==stableJson(inventory.collision)||stableJson(liveGeometry.navObstacles)!==stableJson(inventory.navObstacles))throw new Error('live TMX collision/nav geometry drifts from E1 fixture')
 const fragMap=new Map(fragSpec.fragments.map(f=>[f.stableId,f]))
 const NINE_GRID={northwest:{xMin:0,xMax:555,yMin:0,yMax:309},north_center:{xMin:555,xMax:1110,yMin:0,yMax:309},northeast:{xMin:1110,xMax:1664,yMin:0,yMax:309},west_center:{xMin:0,xMax:555,yMin:309,yMax:618},center:{xMin:555,xMax:1110,yMin:309,yMax:618},east_center:{xMin:1110,xMax:1664,yMin:309,yMax:618},southwest:{xMin:0,xMax:555,yMin:618,yMax:928},south_center:{xMin:555,xMax:1110,yMin:618,yMax:928},southeast:{xMin:1110,xMax:1664,yMin:618,yMax:928}}
 const SIX={ 'west-upper':{xRange:[0,721],yRange:[0,580]},center:{xRange:[721,1130],yRange:[0,580]},'east-upper':{xRange:[1130,1664],yRange:[0,580]},'west-lower':{xRange:[0,721],yRange:[580,928]},entrance:{xRange:[721,1130],yRange:[580,928]},'east-lower':{xRange:[1130,1664],yRange:[580,928]} }
 function geometricRegion(x,y){return Object.entries(NINE_GRID).find(([,r])=>x>=r.xMin&&x<r.xMax&&y>=r.yMin&&y<r.yMax)?.[0]||'center'}
 function futureId(sid){return sid.replace('jyt.occ.','jyt.occluder.').replace('.v2','.v1')}
 function probeRecord(mask,name,point,anchor){const status=pointStatus(point,liveGeometry),inside=pointInPolygonInclusive(point.x,point.y,mask.polygon);return{probeId:`mask-${mask.tmxId}-${name}`,footPoint:point,fixedPointY:Math.round(point.y*256),expectedPainterRelation:name==='behind'?'agent<fragment':'fragment<agent',expectedAgentDrawOrder:name==='behind'?'agent first, fragment after':'fragment first, agent after',boundaryTiePolicy:name==='boundary'?'equal fixedPointY; fragment tieBias=-1 sorts before agent tieBias=0':'not-applicable',insideLegacyPolygon:inside,outsideLegacyPolygonReason:inside?null:`The accepted owner contact line/nearby navigable ground lies outside this legacy polygon slice; probe remains near target and navigation geometry is unchanged.`,navValidation:{source:'live-tmx+e1-equality-checked',navAreaTmxId:NAV_AREA.tmxId,...status},rationale:name==='behind'?`Foot Y ${point.y} is lower than anchor Y ${anchor.y}; normal fixed-point Y order places the agent behind.`:name==='boundary'?`Foot Y exactly equals anchor Y ${anchor.y}; tieBias -1 places fragment before agent.`:`Foot Y ${point.y} is greater than anchor Y ${anchor.y}; normal fixed-point Y order places the agent in front.`}}
 function buildEntry(mask){
  const sid=OWNER_BY_MASK[mask.tmxId],f=fragMap.get(sid);if(!f)throw new Error(`mask ${mask.tmxId} owner missing`)
  const overlaps=fragSpec.fragments.map(candidate=>({stableId:candidate.stableId,ownedPixelsInLegacyPolygon:countOwnedPixelsInPolygon(candidate.ownershipRuns,mask.polygon)})).filter(e=>e.ownedPixelsInLegacyPolygon>0).sort((a,b)=>b.ownedPixelsInLegacyPolygon-a.ownedPixelsInLegacyPolygon||a.stableId.localeCompare(b.stableId))
  const selected=overlaps.find(o=>o.stableId===sid);if(!selected)throw new Error(`mask ${mask.tmxId} target has zero overlap`)
  const fixture=PROBE_FIXTURES[mask.tmxId],recal=RECALIBRATIONS[mask.tmxId]
  const ownerBottom=Math.max(...f.ownershipRuns.map(r=>r[0]))+1
  const recalibratedAnchor=fixture.anchor.y!==ownerBottom
  return {legacyIndex:mask.index,legacyTmxId:mask.tmxId,legacyTmxName:mask.name||`mask-${mask.tmxId}`,polygon:mask.polygon,worldVertices:mask.polygon,polygonVertexCount:mask.polygon.length,aabb:mask.aabb,centroid:mask.centroid,nineGridRegionDeclared:mask.region,nineGridRegionGeometric:geometricRegion(mask.centroid.x,mask.centroid.y),nineGridRegionMatch:mask.regionMatch,homeChunk:f.chunkId,sceneId:'juyiting-main',floorId:'floor-1',chunkId:f.chunkId,targetVisualStructure:VISUAL_BY_MASK[mask.tmxId],targetFragmentStableId:sid,targetFragmentStableIds:[sid],targetFragmentCount:1,targetFragment:{stableId:sid,semanticType:f.semanticType,region:f.region,homeRegion:f.homeRegion,chunkId:f.chunkId,sourceRect:f.sourceRect,ownedOpaquePixelCount:f.ownedOpaquePixelCount},futureOccluderStableId:futureId(sid),futureOccluderStableIds:[futureId(sid)],elevation:0,renderBand:'world',sortMode:'fixed-point-y',sortAnchor:fixture.anchor,fixedPointY:Math.round(fixture.anchor.y*256),tieBias:-1,sortAnchorEvidence:{basis:recalibratedAnchor?'nav-floor-recalibration':'accepted-owner-alpha-contact-line',ownerBottomY:ownerBottom,statement:recalibratedAnchor?`Owner alpha contact ${ownerBottom} is below TMX nav area; test line is calibrated to navigable y=${fixture.anchor.y} without changing nav.`:`Anchor Y equals max accepted ownership row + 1 (${ownerBottom}).`},probes:{behind:probeRecord(mask,'behind',fixture.behind,fixture.anchor),boundary:probeRecord(mask,'boundary',fixture.boundary,fixture.anchor),front:probeRecord(mask,'front',fixture.front,fixture.anchor)},recalibrationDecision:recal?{action:'recalibrate',...recal,proposedPolygon:mask.polygon,evidence:'Retain exact legacy vertices for E10B; region/home metadata is corrected from centroid and accepted owner.'}:'none',constraintDecision:{decision:'none',target:null,relation:null,priority:null,scope:null,rationale:'not-needed: normal fixed-point Y switching and boundary tieBias are sufficient; no constraint edge is required.'},ownerOverlapEvidence:{method:'boundary-inclusive pixel-center test against E9A alpha-rle ownershipRuns',coordinateSpace:'canonical world pixels',ownedPixelsInLegacyPolygon:selected.ownedPixelsInLegacyPolygon,targetOwnedOpaquePixelCount:f.ownedOpaquePixelCount,actualOwners:overlaps,distanceEvidence:{polygonToOwnerPixelDistance:0,zeroDistanceProvenByOverlap:true}},oneToManyRationale:null}
 }
 const entries=inventory.masks.map(buildEntry)
 const prop92=propSpec.props.find(p=>p.tmxId===92);if(!prop92)throw new Error('prop92 missing')
 const m58=entries.find(e=>e.legacyTmxId===58)
 const propPng=PNG.sync.read(read(PATHS.prop92));let propMaskOverlap=0,propOpaque=0
 for(let y=0;y<propPng.height;y++)for(let x=0;x<propPng.width;x++){if(propPng.data[(y*propPng.width+x)*4+3]===0)continue;propOpaque++;if(pointInPolygonInclusive(prop92.tmxRect.x+x+.5,prop92.tmxRect.y+y+.5,m58.polygon))propMaskOverlap++}
 if(propMaskOverlap!==0||propOpaque!==prop92.propAlphaAabbSource.opaquePixels)throw new Error(`prop92 alpha evidence drift overlap=${propMaskOverlap} opaque=${propOpaque}`)
 const tableFixture=(y,expectedByRole,extra={})=>({footPoint:{x:1446,y},fixedPointY:y*256,expectedByRole,navValidation:{source:'live-tmx+e1-equality-checked',navAreaTmxId:NAV_AREA.tmxId,...pointStatus({x:1446,y},liveGeometry)},...extra})
 m58.mask58Evidence={legacyMaskAabb:{minX:1197,minY:342,maxX:1663,maxY:458,width:466,height:116},wallFragmentStableId:m58.targetFragmentStableId,wallOwnerPixelsInPolygon:m58.ownerOverlapEvidence.ownedPixelsInLegacyPolygon,prop92:{stableId:prop92.stableId,tmxId:92,bounds:{x:1360,y:255,width:172,height:124,maxX:1532,maxY:379},assetSha256:prop92.asset.sha256,opaquePixels:prop92.propAlphaAabbSource.opaquePixels,opaquePixelsOverlappingMaskPolygon:propMaskOverlap,sortAnchor:{x:1446,y:379},fixedPointY:97024,tieBias:-4,responsibility:'Independent complete table/board drawable; not owned by mask 58 or its E9A wall fragment.'},roleFixture:{animation:'idle',direction:'down',frameOrdinal:0,cameraZoom:1,cameraDpr:1,navigationContract:'World-order-only fixture, not one of the 111 hard-gated mask probes. The exact requested y=370 sample intersects the table collision/nav obstacle and is retained solely to prove painter order; no navigation semantics are changed.',positions:{behind:tableFixture(370,{lujunyi:'agent<prop',husanniang:'agent<prop'}),boundary:tableFixture(379,{lujunyi:'prop<agent',husanniang:'prop<agent'},{tiePolicy:'equal fixedPointY; prop tieBias -4 sorts before both agents tieBias 0'}),front:tableFixture(420,{lujunyi:'prop<agent',husanniang:'prop<agent'})},regressions:['Both roles use identical world Y facts; Hu Sanniang not incorrectly occluded is an observed sample, not a different coordinate.','Lu Junyi below the table at y=420 renders in front.','Table/agent order is WorldSortKey based, never mask-internal depth scaling.']}}
 const roleEvidence=propSpec.visualEvidence.roles
 const e8aContact=read(PATHS.e8aContact).toString('utf8')
 function frozenRoleFrameUri(role){
  const def=roleEvidence[role], width=String(def.renderedFrameSize.width), marker=`data-role=\"${role}\"`, start=e8aContact.indexOf(marker)
  if(start<0)throw new Error(`E8A contact sheet missing role ${role}`)
  const end=e8aContact.indexOf('</g>',start), group=e8aContact.slice(start,end)
  const match=group.match(new RegExp(`<image href=\"(data:image/png;base64,[^\"]+)\"[^>]*width=\"${width.replace('.', '\\.')}\"`))
  if(!match)throw new Error(`E8A contact sheet missing frozen frame pixels for ${role}`)
  return match[1]
 }
 const roleUris=Object.fromEntries(Object.keys(roleEvidence).map(role=>[role,frozenRoleFrameUri(role)]))
 const inputs={tmx:{path:PATHS.tmx,sha256:sha256(frozen(PATHS.tmx))},inventory:{path:PATHS.inventory,sha256:sha256(read(PATHS.inventory))},e9a:{path:PATHS.fragment,sha256:sha256(read(PATHS.fragment)),generationId:fragSpec.generationId},e9b:{path:PATHS.atlas,sha256:sha256(read(PATHS.atlas)),commit:E9B_COMMIT},e8a:{path:PATHS.prop,sha256:sha256(read(PATHS.prop)),generationId:propSpec.generationId},e8aContact:{path:PATHS.e8aContact,sha256:sha256(read(PATHS.e8aContact)),roleFrameSource:true},e8b:{path:PATHS.e8b,sha256:sha256(read(PATHS.e8b)),tmxSha256:TMX_SHA256},canonical:{path:PATHS.canonical,sha256:sha256(read(PATHS.canonical))},baseHall:{path:PATHS.base,sha256:sha256(read(PATHS.base))},prop92:{path:PATHS.prop92,sha256:sha256(read(PATHS.prop92))},roles:Object.fromEntries(Object.entries(roleEvidence).map(([role,r])=>[role,{assetSha256:r.asset.sha256,frame:r.sourceFrameRect,alphaAabb:r.sourceFrameAlphaAabb,pngSha256:sha256(Buffer.from(roleUris[role].split(',')[1],'base64'))}])),generatorTooling:{generator:{path:'scripts/juyiting/generate-mask-migration-ledger.mjs',sha256:sha256(frozen('scripts/juyiting/generate-mask-migration-ledger.mjs'))},renderer:{path:'scripts/juyiting/render-mask-contact-sheet.mjs',sha256:sha256(frozen('scripts/juyiting/render-mask-contact-sheet.mjs'))},validator:{path:'scripts/juyiting/validate-mask-migration-ledger.mjs',sha256:sha256(frozen('scripts/juyiting/validate-mask-migration-ledger.mjs'))},evidenceLibrary:{path:'scripts/juyiting/lib/mask-migration-evidence.mjs',sha256:sha256(frozen('scripts/juyiting/lib/mask-migration-evidence.mjs'))}},navGeometry:{source:'public/juyiting/hall.tmx',collisionCount:liveGeometry.collision.length,navObstacleCount:liveGeometry.navObstacles.length,e1EqualityChecked:true},contactEvidenceContract:{version:2,markers:['canonical','target-owner','agent-frame','nav-status','prop92-complete'],maskCards:37,probeComposites:111,mask58RoleTableComposites:6,cameraZoom:1,cameraDpr:1}}
 if(inputs.tmx.sha256!==TMX_SHA256||fragSpec.generationId!==E9A_GENERATION_ID)throw new Error('frozen provenance drift')
 const ledger={$schema:'juyiting-occlusion-v2-mask-migration-ledger-v2',schemaVersion:2,taskId:'E10A',baseCommit:BASE_COMMIT,generationId:ZERO_GENERATION_ID,provenance:{tmxSha256:TMX_SHA256,e9aGenerationId:E9A_GENERATION_ID,e9bCommit:E9B_COMMIT,e8aGenerationId:propSpec.generationId,e1BaselineCommit:inventory.baselineCommit,inputHashes:inputs},summary:{totalMasks:37,tmxIdRange:[48,84],recalibrationCount:entries.filter(e=>e.recalibrationDecision!=='none').length,constraintCount:0,oneToManyCount:0,regionMismatchCount:entries.filter(e=>!e.nineGridRegionMatch).length,totalProbeCount:111,probeWarnings:0},navArea:NAV_AREA,nineGridMap:NINE_GRID,sixRegionMap:SIX,entries,contentSha256:''}
 const generationBasis={ledger:{...ledger,generationId:ZERO_GENERATION_ID,contentSha256:''},evidenceInputs:inputs}
 ledger.generationId=createHash('sha256').update(stableJson(generationBasis)).digest('hex')
 ledger.contentSha256=createHash('sha256').update(stableJson({...ledger,contentSha256:''})).digest('hex')
 const contact=renderMaskContactSheet({ledger,fragSpec,canonicalBytes:read(PATHS.canonical),baseBytes:read(PATHS.base),prop92Bytes:read(PATHS.prop92),roleEvidence,roleUris})
 if(write){atomicWriteUtf8Batch([{path:join(REPO_ROOT,PATHS.ledger),content:stableJson(ledger)+'\n',label:'E10A ledger'},{path:join(REPO_ROOT,PATHS.contact),content:contact,label:'E10A contact sheet'}],'E10A deterministic fixtures')}
 return {ledger,contact,ledgerText:stableJson(ledger)+'\n'}
}

async function cli(){const result=await generateMaskMigrationFixtures({write:true});console.log(`Generated 37/37 masks atomically; generationId=${result.ledger.generationId}; constraints=0; recalibrations=${result.ledger.summary.recalibrationCount}; warnings=0`)}
if(process.argv[1]===fileURLToPath(import.meta.url))cli()
